{
  "name": "wps-wpp-ts-esbuild-hello",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WPS 演示 Hello World 加载项（TypeScript + esbuild）+ wps-js-addin-debug-bridge 接入示例",
  "scripts": {
    "dev": "node dev-server.js",
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "test:wps": "wps-bridge run ./test/hello.test.ts --expect-report --start \"npm run dev\""
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "wps-js-addin-debug-bridge": "file:../.."
  }
}
